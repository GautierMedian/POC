apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "csv-mapper.fullname" . }}
  labels:
    {{- include "csv-mapper.labels" . | nindent 4 }}
data:
  # Add any configuration data here if needed
  # For example, environment-specific settings
